{
  "name": "xcryptocap",
  "version": "1.0.0",
  "description": "API pour XcryptoCap",
  "main": "api/index.js",
  "scripts": {
    "build": "mkdir -p public && cp index.html khacn.html strb.html robots.txt sitemap.xml manifest.json public/",
    "start": "node api/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.2.2"
  }
}
